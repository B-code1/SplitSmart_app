import { Text } from 'react-native';
import { ScrollView } from "react-native-web";

const Groups = () => {

    <ScrollView>
        <Text> Groups</Text>

    </ScrollView>

};

export default Groups